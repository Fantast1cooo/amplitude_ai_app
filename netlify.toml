
[build]
  command = "npm install"
  functions = "netlify/functions/"
  publish = "public/"

[[redirects]]
  from = "/api/generate" # Точный путь, который запрашивает frontend
  to = "/.netlify/functions/generate" # Точный путь к нашей новой функции
  status = 200